# Core crypto/runtime
argon2-cffi>=23.1
pynacl>=1.5
reedsolo>=1.7
psutil>=5.9

# UI (optional if using the KeyGuard/GUI)
PySide6>=6.6
ttkbootstrap>=1.10

# Optional crypto (used elsewhere; no longer required by HKDF mixing path)
cryptography>=42
